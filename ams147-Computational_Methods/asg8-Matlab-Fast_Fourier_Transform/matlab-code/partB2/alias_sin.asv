% This code plots different sin functions
% g1(t) = sin(t)        t = 2*pi*[0:N]/N, N = 500
% g2(t) = sin(21t)      t = 2*pi*[0:N]/N, N = 500
% g3(t) = sin(21t)      t = 2*pi*[0:N]/N, N = 20
%
clear
figure(5)
clf reset
axes('position',[0.15,0.13,0.75,0.75])
%
N1=500; N2=20;
t1=2*pi*[0:N1]/N1; t2=2*pi*[0
g1=sin(t);